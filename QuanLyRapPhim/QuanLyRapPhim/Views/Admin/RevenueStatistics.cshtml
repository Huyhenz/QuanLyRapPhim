@model dynamic
@using Newtonsoft.Json
@using System.Collections.Generic
@using System.Globalization
@using System.Linq
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@{
    ViewData["Title"] = Localizer["RevenueStatistics"].Value;
    Layout = "~/Views/Shared/_Layout.cshtml";
    bool isAdmin = User.IsInRole("Admin");
}
<div class="container-fluid d-flex position-relative">
    <!-- Toggle Button -->
    @if (isAdmin)
    {
        <button id="sidebar-toggle" class="sidebar-toggle">
            <i class="fas fa-bars"></i>
        </button>
    }
    <!-- SIDEBAR -->
    @if (isAdmin)
    {
        <aside class="sidebar">
            <div class="sidebar-header">
                <h3 class="sidebar-title">🎬 @Localizer["CinemaXAdmin"]</h3>
            </div>
            <nav class="sidebar-nav">
                <a href="/Admin/Index"
                   class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Admin" && ViewContext.RouteData.Values["Action"]?.ToString() == "Index" ? "active" : "")">
                    <i class="fas fa-tachometer-alt"></i> @Localizer["Dashboard"]
                </a>
                <a href="/Movies"
                   class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Movies" ? "active" : "")">
                    <i class="fas fa-film"></i> @Localizer["ManageMovies"]
                </a>
                <a href="/Showtimes"
                   class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Showtimes" ? "active" : "")">
                    <i class="fas fa-clock"></i> @Localizer["ManageShowtimes"]
                </a>
                <a href="/Rooms"
                   class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Rooms" ? "active" : "")">
                    <i class="fas fa-door-open"></i> @Localizer["ManageRooms"]
                </a>
                <a href="/Seats"
                   class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Seats" ? "active" : "")">
                    <i class="fas fa-chair"></i> @Localizer["ManageSeats"]
                </a>
                <a href="/Admin/ManageFoods"
                   class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "ManageFoods" ? "active" : "")">
                    <i class="fas fa-utensils"></i> @Localizer["ManageFoods"]
                </a>
                <a href="/Admin/ManageBookings"
                   class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Admin" && ViewContext.RouteData.Values["Action"]?.ToString() == "ManageBookings" ? "active" : "")">
                    <i class="fas fa-ticket-alt"></i> @Localizer["ManageBookings"]
                </a>
                <a href="/Admin/RevenueStatistics"
                   class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Admin" && ViewContext.RouteData.Values["Action"]?.ToString() == "RevenueStatistics" ? "active" : "")">
                    <i class="fas fa-chart-bar"></i> @Localizer["RevenueStatistics"]
                </a>
                <a href="/Vouchers"
                   class="nav-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Vouchers" ? "active" : "")">
                    <i class="fas fa-gift"></i> @Localizer["ManageVouchers"]
                </a>
                <div class="nav-divider"></div>
                <a href="#" class="nav-item"><i class="fab fa-facebook"></i> Facebook</a>
                <a href="#" class="nav-item"><i class="fab fa-twitter"></i> Twitter</a>
                <a href="#" class="nav-item"><i class="fab fa-google"></i> Google</a>
            </nav>
        </aside>
    }
    <!-- MAIN CONTENT -->
    <div class="frame-wrapper flex-grow-1 @(isAdmin ? "admin-view" : "")">
        <div class="content-inner">
            <h2 class="page-title">@Localizer["RevenueStatistics"]</h2>
            <div class="header-actions">
                <span class="order-count">@Localizer["RevenueDataOverview"]</span>
                <div class="date-filter">
                    <form id="dateFilterForm" method="get" action="@Url.Action("RevenueStatistics")">
                        <input type="hidden" name="fromDate" id="fromDateInput" value="@(((dynamic)ViewBag.RevenueData)?.FromDate ?? DateTime.Now.AddMonths(-1).ToString("yyyy-MM-dd"))">
                        <input type="hidden" name="toDate" id="toDateInput" value="@(((dynamic)ViewBag.RevenueData)?.ToDate ?? DateTime.Now.ToString("yyyy-MM-dd"))">
                        <span>@Localizer["From"]</span>
                        <input type="date" class="date-input" id="fromDate" onchange="updateFilter()">
                        <span>@Localizer["To"]</span>
                        <input type="date" class="date-input" id="toDate" onchange="updateFilter()">
                        <button type="submit" class="filter-btn">@Localizer["Filter"]</button>
                    </form>
                </div>
            </div>
            <!-- Alerts -->
            <div id="alert-container" class="mb-4"></div>
            <!-- Revenue Summary with Quantity -->
            <div class="admin-section">
                <h3 class="section-subtitle">@Localizer["Summary"]</h3>
                <div class="summary-grid">
                    <div class="summary-card-item">
                        <div class="summary-icon">🎟️</div>
                        <div class="summary-details">
                            <h4>@Localizer["TotalTicketRevenue"]</h4>
                            <p class="revenue-amount">@(((dynamic)ViewBag.RevenueData)?.TotalTicketRevenue.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</p>
                            <p class="quantity-label">Số lượng vé: <strong>@(((dynamic)ViewBag.RevenueData)?.TotalTicketCount ?? 0)</strong></p>
                        </div>
                    </div>
                    <div class="summary-card-item">
                        <div class="summary-icon">🍿</div>
                        <div class="summary-details">
                            <h4>@Localizer["TotalFoodRevenue"]</h4>
                            <p class="revenue-amount">@(((dynamic)ViewBag.RevenueData)?.TotalFoodRevenue.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</p>
                            <p class="quantity-label">Số lượng: <strong>@(((dynamic)ViewBag.RevenueData)?.TotalFoodCount ?? 0)</strong></p>
                        </div>
                    </div>
                    <div class="summary-card-item">
                        <div class="summary-icon">🥤</div>
                        <div class="summary-details">
                            <h4>@Localizer["TotalDrinkRevenue"]</h4>
                            <p class="revenue-amount">@(((dynamic)ViewBag.RevenueData)?.TotalDrinkRevenue.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</p>
                            <p class="quantity-label">Số lượng: <strong>@(((dynamic)ViewBag.RevenueData)?.TotalDrinkCount ?? 0)</strong></p>
                        </div>
                    </div>
                    <div class="summary-card-item">
                        <div class="summary-icon">🎁</div>
                        <div class="summary-details">
                            <h4>@Localizer["TotalComboRevenue"]</h4>
                            <p class="revenue-amount">@(((dynamic)ViewBag.RevenueData)?.TotalComboRevenue.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</p>
                            <p class="quantity-label">Số lượng: <strong>@(((dynamic)ViewBag.RevenueData)?.TotalComboCount ?? 0)</strong></p>
                        </div>
                    </div>
                    <div class="summary-card-item highlight">
                        <div class="summary-icon">💰</div>
                        <div class="summary-details">
                            <h4>@Localizer["TotalRevenue"]</h4>
                            <p class="revenue-amount-large">@(((dynamic)ViewBag.RevenueData)?.TotalRevenue.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</p>
                            <p class="quantity-label">Tổng đơn hàng: <strong>@(((dynamic)ViewBag.RevenueData)?.TotalBookingCount ?? 0)</strong></p>
                        </div>
                    </div>
                </div>
            </div>
            <!-- Detailed Food Item Statistics -->
            <div class="admin-section">
                <h3 class="section-subtitle">📊 Thống Kê Chi Tiết Món Ăn/Thức Uống</h3>
                <div class="table-responsive">
                    <table class="table table-custom">
                        <thead>
                            <tr>
                                <th>Tên món</th>
                                <th>Danh mục</th>
                                <th>Số lượng bán</th>
                                <th>Doanh thu (VND)</th>
                                <th>Giá trung bình (VND)</th>
                            </tr>
                        </thead>
                        <tbody>
                            @{
                                var foodStats = ((dynamic)ViewBag.RevenueData)?.FoodItemStatistics as IEnumerable<dynamic> ?? new List<dynamic>();
                                if (foodStats.Any())
                                {
                                    foreach (var item in foodStats)
                                    {
                                        <tr>
                                            <td>@(item.Name ?? Localizer["N/A"].Value)</td>
                                            <td>
                                                @if (item.Category == "Food")
                                                {
                                                    <span class="badge badge-food">🍿 Thức ăn</span>
                                                }
                                                else if (item.Category == "Drink")
                                                {
                                                    <span class="badge badge-drink">🥤 Thức uống</span>
                                                }
                                                else if (item.Category == "Combo")
                                                {
                                                    <span class="badge badge-combo">🎁 Combo</span>
                                                }
                                                else
                                                {
                                                    <span>@(item.Category ?? Localizer["N/A"].Value)</span>
                                                }
                                            </td>
                                            <td><strong>@(item.TotalQuantity)</strong></td>
                                            <td>@(item.TotalRevenue?.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</td>
                                            <td>@(item.AveragePrice?.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr><td colspan="5" class="text-center">@Localizer["NoData"].Value</td></tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
            <!-- Revenue by Movie -->
            <div class="admin-section">
                <h3 class="section-subtitle">@Localizer["RevenueByMovie"]</h3>
                <div class="table-responsive">
                    <table class="table table-custom">
                        <thead>
                            <tr>
                                <th>@Localizer["MovieTitle"]</th>
                                <th>@Localizer["TicketRevenue"] (VND)</th>
                                <th>@Localizer["TotalRevenue"] (VND)</th>
                            </tr>
                        </thead>
                        <tbody>
                            @{
                                var revenueByMovie = ((dynamic)ViewBag.RevenueData)?.RevenueByMovie as IEnumerable<dynamic> ?? new List<dynamic>();
                                if (revenueByMovie.Any())
                                {
                                    foreach (var item in revenueByMovie)
                                    {
                                        <tr>
                                            <td>@(item.MovieTitle ?? Localizer["N/A"].Value)</td>
                                            <td>@(item.TicketRevenue?.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</td>
                                            <td>@(item.TotalRevenue?.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr><td colspan="3" class="text-center">@Localizer["NoData"].Value</td></tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
            <!-- Revenue by Date with Quantity -->
            <div class="admin-section">
                <h3 class="section-subtitle">@Localizer["RevenueByDate"]</h3>
                <div class="table-responsive">
                    <table class="table table-custom">
                        <thead>
                            <tr>
                                <th>@Localizer["Date"]</th>
                                <th>Vé (VND / SL)</th>
                                <th>Thức ăn (VND / SL)</th>
                                <th>Thức uống (VND / SL)</th>
                                <th>Combo (VND / SL)</th>
                                <th>@Localizer["Total"] (VND)</th>
                            </tr>
                        </thead>
                        <tbody>
                            @{
                                var revenueByDate = ((dynamic)ViewBag.RevenueData)?.RevenueByDate as IEnumerable<dynamic> ?? new List<dynamic>();
                                if (revenueByDate.Any())
                                {
                                    foreach (var item in revenueByDate)
                                    {
                                        <tr>
                                            <td>@(((dynamic)item).Date?.ToString("dd/MM/yyyy") ?? Localizer["N/A"].Value)</td>
                                            <td>
                                                <div>@(((dynamic)item).TicketRevenue?.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</div>
                                                <small class="text-muted">SL: @(((dynamic)item).TicketCount ?? 0)</small>
                                            </td>
                                            <td>
                                                <div>@(((dynamic)item).FoodRevenue?.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</div>
                                                <small class="text-muted">SL: @(((dynamic)item).FoodCount ?? 0)</small>
                                            </td>
                                            <td>
                                                <div>@(((dynamic)item).DrinkRevenue?.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</div>
                                                <small class="text-muted">SL: @(((dynamic)item).DrinkCount ?? 0)</small>
                                            </td>
                                            <td>
                                                <div>@(((dynamic)item).ComboRevenue?.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</div>
                                                <small class="text-muted">SL: @(((dynamic)item).ComboCount ?? 0)</small>
                                            </td>
                                            <td><strong>@(((dynamic)item).TotalRevenue?.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</strong></td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr><td colspan="6" class="text-center">@Localizer["NoData"].Value</td></tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
            <!-- Revenue by Month Table -->
            <div class="admin-section">
                <h3 class="section-subtitle">@Localizer["RevenueByMonth"]</h3>
                <div class="table-responsive">
                    <table class="table table-custom">
                        <thead>
                            <tr>
                                <th>@Localizer["Month"]</th>
                                <th>@Localizer["TicketRevenue"] (VND)</th>
                                <th>@Localizer["FoodRevenue"] (VND)</th>
                                <th>@Localizer["DrinkRevenue"] (VND)</th>
                                <th>@Localizer["ComboRevenue"] (VND)</th>
                                <th>@Localizer["Total"] (VND)</th>
                            </tr>
                        </thead>
                        <tbody>
                            @{
                                var revenueByMonth = ((dynamic)ViewBag.RevenueData)?.RevenueByMonth as IEnumerable<dynamic> ?? new List<dynamic>();
                                if (revenueByMonth.Any())
                                {
                                    foreach (var item in revenueByMonth)
                                    {
                                        <tr>
                                            <td>@(((dynamic)item).MonthName + " " + ((dynamic)item).Year)</td>
                                            <td>@(((dynamic)item).TicketRevenue?.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</td>
                                            <td>@(((dynamic)item).FoodRevenue?.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</td>
                                            <td>@(((dynamic)item).DrinkRevenue?.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</td>
                                            <td>@(((dynamic)item).ComboRevenue?.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</td>
                                            <td>@(((dynamic)item).TotalRevenue?.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr><td colspan="6" class="text-center">@Localizer["NoData"].Value</td></tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
            <!-- Food Revenue by Category by Date with Quantity -->
            <div class="admin-section">
                <h3 class="section-subtitle">@Localizer["FoodRevenueByCategoryAndDate"]</h3>
                <div class="table-responsive">
                    <table class="table table-custom">
                        <thead>
                            <tr>
                                <th>@Localizer["Date"]</th>
                                <th>@Localizer["Category"]</th>
                                <th>Số lượng</th>
                                <th>@Localizer["Revenue"] (VND)</th>
                            </tr>
                        </thead>
                        <tbody>
                            @{
                                var formattedFoodByDate = ViewBag.FormattedFoodByDate?.ToString() ?? "[]";
                                var foodByDate = JsonConvert.DeserializeObject<List<dynamic>>(formattedFoodByDate);
                                if (foodByDate != null && foodByDate.Count > 0)
                                {
                                    foreach (var dateGroup in foodByDate)
                                    {
                                        dynamic dg = dateGroup;
                                        var categories = dg.Categories as IEnumerable<dynamic>;
                                        if (categories != null)
                                        {
                                            foreach (var item in categories)
                                            {
                                                <tr>
                                                    <td>@(dg.Date?.ToString("dd/MM/yyyy") ?? Localizer["N/A"].Value)</td>
                                                    <td>
                                                        @if (item.Category == "Food")
                                                        {
                                                            <span class="badge badge-food">🍿 Thức ăn</span>
                                                        }
                                                        else if (item.Category == "Drink")
                                                        {
                                                            <span class="badge badge-drink">🥤 Thức uống</span>
                                                        }
                                                        else if (item.Category == "Combo")
                                                        {
                                                            <span class="badge badge-combo">🎁 Combo</span>
                                                        }
                                                        else
                                                        {
                                                            <span>@(item.Category ?? Localizer["N/A"].Value)</span>
                                                        }
                                                    </td>
                                                    <td><strong>@(item.Quantity ?? 0)</strong></td>
                                                    <td>@(item.Revenue?.ToString("C", new System.Globalization.CultureInfo("vi-VN")) ?? Localizer["N/A"].Value)</td>
                                                </tr>
                                            }
                                        }
                                    }
                                }
                                else
                                {
                                    <tr><td colspan="4" class="text-center">@Localizer["NoData"].Value</td></tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
            <!-- Charts -->
            <div class="admin-section">
                <h3 class="section-subtitle">@Localizer["RevenueChartByDate"]</h3>
                <canvas id="revenueByDateChart" width="400" height="200"></canvas>
            </div>
            <div class="admin-section">
                <h3 class="section-subtitle">@Localizer["RevenueChartByMonth"]</h3>
                <canvas id="revenueByMonthChart" width="400" height="200"></canvas>
            </div>
            <!-- Additional Small Charts for Monthly Revenue -->
            <div class="admin-section">
                <h3 class="section-subtitle">@Localizer["MonthlyTicketChart"]</h3>
                <canvas id="monthlyTicketChart" width="300" height="150"></canvas>
            </div>
            <div class="admin-section">
                <h3 class="section-subtitle">@Localizer["MonthlyFoodChart"]</h3>
                <canvas id="monthlyFoodChart" width="300" height="150"></canvas>
            </div>
            <div class="admin-section">
                <h3 class="section-subtitle">@Localizer["MonthlyDrinkChart"]</h3>
                <canvas id="monthlyDrinkChart" width="300" height="150"></canvas>
            </div>
            <div class="admin-section">
                <h3 class="section-subtitle">@Localizer["MonthlyComboChart"]</h3>
                <canvas id="monthlyComboChart" width="300" height="150"></canvas>
            </div>
        </div>
    </div>
</div>
<style>
    body {
        margin: 0;
        background-color: #0F0F0F; /* Galaxy Black */
        color: #ffffff;
        font-family: 'Montserrat', sans-serif;
        overflow-x: hidden;
    }
    /* ===== SIDEBAR STYLE (GIỐNG SHOWTIME) ===== */
    .sidebar {
        background: #111;
        width: 250px;
        height: 100vh;
        position: fixed;
        top: 0;
        left: 0;
        padding: 25px 20px;
        overflow-y: auto;
        border-right: 2px solid #e50914;
        box-shadow: 2px 0 10px rgba(0,0,0,0.4);
        transition: width 0.3s ease, transform 0.3s ease;
        z-index: 1000;
    }

        .sidebar.collapsed {
            width: 0;
            padding: 0;
            transform: translateX(-100%);
        }

    .sidebar-title {
        color: #fff;
        font-weight: 700;
        font-size: 1.4rem;
    }

    .sidebar-nav .nav-item {
        color: #ddd;
        padding: 12px 15px;
        border-radius: 8px;
        margin-bottom: 5px;
        font-size: 1rem;
        display: flex;
        align-items: center;
        gap: 12px;
        text-decoration: none;
        transition: 0.25s;
    }

    .nav-item i {
        width: 20px;
    }

    .nav-item:hover {
        background-color: #e50914;
        color: #fff;
        transform: translateX(5px);
    }

    .nav-item.active {
        background-color: #e50914;
        color: #fff;
        font-weight: 600;
    }

    .nav-divider {
        height: 1px;
        background: #444;
        margin: 15px 0;
    }

    .frame-wrapper.admin-view {
        margin-left: 250px;
        transition: margin-left 0.3s ease;
    }

        .frame-wrapper.admin-view.shifted {
            margin-left: 0;
        }

    .content-inner {
        max-width: 1200px;
        width: 100%;
    }

    .page-title {
        background-color: #1c1c1c;
        color: #fff;
        padding: 15px;
        text-align: center;
        border-radius: 15px;
        font-family: 'Montserrat', sans-serif;
        font-weight: 700;
        text-transform: uppercase;
        margin-bottom: 20px;
    }

    .header-actions {
        display: flex;
        align-items: center;
        gap: 15px;
        margin-bottom: 20px;
    }

    .order-count {
        font-size: 0.9rem;
        color: #D3D3D3;
    }

    .date-filter {
        display: flex;
        align-items: center;
        gap: 5px;
        color: #ffffff;
    }

    .date-input {
        padding: 8px;
        border-radius: 12px;
        border: 1px solid #ff0033;
        background-color: #1c1c1c;
        color: #ffffff;
    }

        .date-input:hover {
            transform: scale(1.05);
            transition: transform 0.3s ease;
        }

    .admin-section {
        margin-bottom: 50px;
    }

    .section-subtitle {
        font-size: 1.5rem;
        font-weight: 600;
        margin-bottom: 20px;
        color: #ff0033; /* Red text */
    }

    .table-custom {
        width: 100%;
        background: #1c1c1c;
        border: 1px solid #ff0033;
        border-radius: 15px;
        overflow: hidden;
        table-layout: fixed;
    }

        .table-custom th, .table-custom td {
            padding: 12px;
            text-align: left;
            vertical-align: middle;
            word-wrap: break-word;
            border: 1px solid #ff0033;
        }

        .table-custom th {
            background: #000000;
            color: #ffffff;
            font-weight: 600;
        }

        .table-custom td {
            background: #1c1c1c;
            color: #ffffff;
        }

        .table-custom tr:hover td {
            background: #ff0033;
            color: #000000;
        }

        .table-custom td small.text-muted {
            color: #999;
            display: block;
            margin-top: 3px;
        }

    .summary-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
        margin-bottom: 30px;
    }

    .summary-card-item {
        background: linear-gradient(135deg, #1c1c1c 0%, #2a2a2a 100%);
        padding: 20px;
        border-radius: 15px;
        border: 2px solid #ff0033;
        display: flex;
        align-items: center;
        gap: 15px;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

        .summary-card-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(255, 0, 51, 0.3);
        }

        .summary-card-item.highlight {
            background: linear-gradient(135deg, #ff0033 0%, #cc0026 100%);
            border-color: #ffffff;
        }

    .summary-icon {
        font-size: 3rem;
        min-width: 60px;
        text-align: center;
    }

    .summary-details h4 {
        color: #ffffff;
        font-size: 0.9rem;
        margin: 0 0 8px 0;
        text-transform: uppercase;
    }

    .revenue-amount {
        color: #4CAF50;
        font-size: 1.5rem;
        font-weight: 700;
        margin: 5px 0;
    }

    .revenue-amount-large {
        color: #ffffff;
        font-size: 2rem;
        font-weight: 700;
        margin: 5px 0;
    }

    .quantity-label {
        color: #d3d3d3;
        font-size: 0.85rem;
        margin: 5px 0 0 0;
    }

        .quantity-label strong {
            color: #ffffff;
            font-size: 1.1rem;
        }

    .badge {
        padding: 5px 12px;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 600;
    }

    .badge-food {
        background: #FFA726;
        color: #000;
    }

    .badge-drink {
        background: #42A5F5;
        color: #fff;
    }

    .badge-combo {
        background: #AB47BC;
        color: #fff;
    }

    #revenueChart {
        max-width: 100%;
        height: auto !important;
        background: #1c1c1c;
        border-radius: 15px;
        padding: 10px;
    }

    .alert {
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
    }

    .alert-success {
        background-color: #1c1c1c;
        color: #28a745;
        border-color: #28a745;
    }

    .alert-danger {
        background-color: #1c1c1c;
        color: #dc3545;
        border-color: #dc3545;
    }

    .filter-btn {
        padding: 8px 12px;
        background: #ff0033;
        color: #000;
        border: none;
        border-radius: 12px;
        cursor: pointer;
        margin-left: 10px;
    }

        .filter-btn:hover {
            background: #cc0026;
        }
    /* Toggle Button - Compact and Beautiful */
    .sidebar-toggle {
        position: fixed;
        top: 15px;
        left: 15px;
        z-index: 1001;
        background: linear-gradient(135deg, #ff0033, #e50914);
        color: #fff;
        border: none;
        border-radius: 50%;
        width: 35px;
        height: 35px;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 4px 15px rgba(229, 9, 20, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.1);
        transition: all 0.3s ease;
        cursor: pointer;
        font-size: 14px;
    }

        .sidebar-toggle:hover {
            transform: scale(1.05) rotate(5deg);
            box-shadow: 0 6px 20px rgba(229, 9, 20, 0.6), inset 0 1px 0 rgba(255, 255, 255, 0.1);
        }

        .sidebar-toggle:active {
            transform: scale(0.95);
        }

    .sidebar.collapsed + .frame-wrapper.admin-view .sidebar-toggle {
        left: 15px;
    }
    /* Media Queries */
    @@media (max-width: 768px) {
        .sidebar {
            width: 200px;
        }

        .frame-wrapper.admin-view {
            margin-left: 200px;
        }

        .sidebar.collapsed {
            width: 0;
            padding: 0;
            transform: translateX(-100%);
        }

        .frame-wrapper.admin-view.shifted {
            margin-left: 0;
        }

        .sidebar-toggle {
            top: 10px;
            left: 10px;
            width: 32px;
            height: 32px;
            font-size: 13px;
        }
    }

    @@media (max-width: 576px) {
        .container-fluid.d-flex {
            flex-direction: column;
        }

        .sidebar {
            position: relative;
            width: 100%;
            height: auto;
        }

        .frame-wrapper.admin-view {
            margin-left: 0;
            width: 100%;
            min-height: auto;
        }

        .content-inner {
            width: 100%;
            padding: 20px;
        }

        .sidebar.collapsed {
            display: none;
        }

        .sidebar-toggle {
            position: relative;
            margin: 10px auto;
            display: block;
        }
    }
</style>
@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const toggleBtn = document.getElementById('sidebar-toggle');
            const sidebar = document.querySelector('.sidebar');
            const frameWrapper = document.querySelector('.frame-wrapper');
            if (toggleBtn && sidebar && frameWrapper) {
                toggleBtn.addEventListener('click', function() {
                    sidebar.classList.toggle('collapsed');
                    frameWrapper.classList.toggle('shifted');
                    const icon = toggleBtn.querySelector('i');
                    if (sidebar.classList.contains('collapsed')) {
                        icon.className = 'fas fa-bars';
                    } else {
                        icon.className = 'fas fa-times';
                    }
                });
            }
        });
        $(document).ready(function () {
            // Set initial date values
            $('#fromDate').val('@(((dynamic)ViewBag.RevenueData)?.FromDate ?? DateTime.Now.AddMonths(-1).ToString("yyyy-MM-dd"))');
            $('#toDate').val('@(((dynamic)ViewBag.RevenueData)?.ToDate ?? DateTime.Now.ToString("yyyy-MM-dd"))');
            // Update hidden inputs on date change
            function updateFilter() {
                $('#fromDateInput').val($('#fromDate').val());
                $('#toDateInput').val($('#toDate').val());
            }
            // Parse JSON data for charts
            var revenueByDateData = JSON.parse('@Html.Raw(ViewBag.FormattedRevenueByDate ?? "[]")');
            var revenueByMonthData = JSON.parse('@Html.Raw(ViewBag.FormattedRevenueByMonth ?? "[]")');
            // Chart theo ngày
            if (revenueByDateData.length > 0) {
                var ctxDate = document.getElementById('revenueByDateChart').getContext('2d');
                new Chart(ctxDate, {
                    type: 'bar',
                    data: {
                        labels: revenueByDateData.map(item => item.Date),
                        datasets: [
                            {
                                label: '@Localizer["TicketRevenue"] (VND)',
                                data: revenueByDateData.map(item => item.TicketRevenue),
                                backgroundColor: 'rgba(0, 123, 255, 0.2)',
                                borderColor: 'rgba(0, 123, 255, 1)',
                                borderWidth: 1
                            },
                            {
                                label: '@Localizer["FoodRevenue"] (VND)',
                                data: revenueByDateData.map(item => item.FoodRevenue),
                                backgroundColor: 'rgba(255, 193, 7, 0.2)',
                                borderColor: 'rgba(255, 193, 7, 1)',
                                borderWidth: 1
                            },
                            {
                                label: '@Localizer["DrinkRevenue"] (VND)',
                                data: revenueByDateData.map(item => item.DrinkRevenue),
                                backgroundColor: 'rgba(40, 167, 69, 0.2)',
                                borderColor: 'rgba(40, 167, 69, 1)',
                                borderWidth: 1
                            },
                            {
                                label: '@Localizer["ComboRevenue"] (VND)',
                                data: revenueByDateData.map(item => item.ComboRevenue),
                                backgroundColor: 'rgba(153, 102, 255, 0.2)',
                                borderColor: 'rgba(153, 102, 255, 1)',
                                borderWidth: 1
                            },
                            {
                                label: '@Localizer["Total"] (VND)',
                                data: revenueByDateData.map(item => item.TotalRevenue),
                                backgroundColor: 'rgba(255, 0, 51, 0.2)',
                                borderColor: 'rgba(255, 0, 51, 1)',
                                borderWidth: 1
                            }
                        ]
                    },
                    options: {
                        responsive: true,
                        scales: {
                            y: {
                                beginAtZero: true,
                                ticks: {
                                    callback: function(value) {
                                        return new Intl.NumberFormat('vi-VN', { style: 'currency', currency: 'VND' }).format(value);
                                    },
                                    color: '#FFFFFF'
                                },
                                grid: { color: 'rgba(255, 255, 255, 0.1)' }
                            },
                            x: {
                                ticks: { color: '#FFFFFF' },
                                grid: { color: 'rgba(255, 255, 255, 0.1)' }
                            }
                        },
                        plugins: {
                            legend: { labels: { color: '#FFFFFF' } }
                        }
                    }
                });
            }
            // Chart theo tháng
            if (revenueByMonthData.length > 0) {
                var ctxMonth = document.getElementById('revenueByMonthChart').getContext('2d');
                new Chart(ctxMonth, {
                    type: 'line',
                    data: {
                        labels: revenueByMonthData.map(item => item.Month),
                        datasets: [
                            {
                                label: '@Localizer["TicketRevenue"] (VND)',
                                data: revenueByMonthData.map(item => item.TicketRevenue),
                                borderColor: 'rgba(0, 123, 255, 1)',
                                backgroundColor: 'rgba(0, 123, 255, 0.2)',
                                tension: 0.1
                            },
                            {
                                label: '@Localizer["FoodRevenue"] (VND)',
                                data: revenueByMonthData.map(item => item.FoodRevenue),
                                borderColor: 'rgba(255, 193, 7, 1)',
                                backgroundColor: 'rgba(255, 193, 7, 0.2)',
                                tension: 0.1
                            },
                            {
                                label: '@Localizer["DrinkRevenue"] (VND)',
                                data: revenueByMonthData.map(item => item.DrinkRevenue),
                                borderColor: 'rgba(40, 167, 69, 1)',
                                backgroundColor: 'rgba(40, 167, 69, 0.2)',
                                tension: 0.1
                            },
                            {
                                label: '@Localizer["ComboRevenue"] (VND)',
                                data: revenueByMonthData.map(item => item.ComboRevenue),
                                borderColor: 'rgba(153, 102, 255, 1)',
                                backgroundColor: 'rgba(153, 102, 255, 0.2)',
                                tension: 0.1
                            },
                            {
                                label: '@Localizer["Total"] (VND)',
                                data: revenueByMonthData.map(item => item.TotalRevenue),
                                borderColor: 'rgba(255, 0, 51, 1)',
                                backgroundColor: 'rgba(255, 0, 51, 0.2)',
                                tension: 0.1
                            }
                        ]
                    },
                    options: {
                        responsive: true,
                        scales: {
                            y: {
                                beginAtZero: true,
                                ticks: {
                                    callback: function(value) {
                                        return new Intl.NumberFormat('vi-VN', { style: 'currency', currency: 'VND' }).format(value);
                                    },
                                    color: '#FFFFFF'
                                },
                                grid: { color: 'rgba(255, 255, 255, 0.1)' }
                            },
                            x: {
                                ticks: { color: '#FFFFFF' },
                                grid: { color: 'rgba(255, 255, 255, 0.1)' }
                            }
                        },
                        plugins: {
                            legend: { labels: { color: '#FFFFFF' } }
                        }
                    }
                });
            }
            // Biểu đồ nhỏ - Doanh thu vé theo tháng
            if (revenueByMonthData.length > 0) {
                var ctxTicket = document.getElementById('monthlyTicketChart').getContext('2d');
                new Chart(ctxTicket, {
                    type: 'bar',
                    data: {
                        labels: revenueByMonthData.map(item => item.Month),
                        datasets: [{
                            label: '@Localizer["TicketRevenue"] (VND)',
                            data: revenueByMonthData.map(item => item.TicketRevenue),
                            backgroundColor: 'rgba(0, 123, 255, 0.6)',
                            borderColor: 'rgba(0, 123, 255, 1)',
                            borderWidth: 1
                        }]
                    },
                    options: {
                        responsive: true,
                        scales: {
                            y: {
                                beginAtZero: true,
                                ticks: {
                                    callback: function(value) {
                                        return new Intl.NumberFormat('vi-VN', { style: 'currency', currency: 'VND' }).format(value);
                                    },
                                    color: '#FFFFFF'
                                },
                                grid: { color: 'rgba(255, 255, 255, 0.1)' }
                            },
                            x: {
                                ticks: { color: '#FFFFFF' },
                                grid: { color: 'rgba(255, 255, 255, 0.1)' }
                            }
                        },
                        plugins: {
                            legend: { labels: { color: '#FFFFFF' } }
                        }
                    }
                });
            }
            // Biểu đồ nhỏ - Doanh thu thức ăn theo tháng
            if (revenueByMonthData.length > 0) {
                var ctxFood = document.getElementById('monthlyFoodChart').getContext('2d');
                new Chart(ctxFood, {
                    type: 'bar',
                    data: {
                        labels: revenueByMonthData.map(item => item.Month),
                        datasets: [{
                            label: '@Localizer["FoodRevenue"] (VND)',
                            data: revenueByMonthData.map(item => item.FoodRevenue),
                            backgroundColor: 'rgba(255, 193, 7, 0.6)',
                            borderColor: 'rgba(255, 193, 7, 1)',
                            borderWidth: 1
                        }]
                    },
                    options: {
                        responsive: true,
                        scales: {
                            y: {
                                beginAtZero: true,
                                ticks: {
                                    callback: function(value) {
                                        return new Intl.NumberFormat('vi-VN', { style: 'currency', currency: 'VND' }).format(value);
                                    },
                                    color: '#FFFFFF'
                                },
                                grid: { color: 'rgba(255, 255, 255, 0.1)' }
                            },
                            x: {
                                ticks: { color: '#FFFFFF' },
                                grid: { color: 'rgba(255, 255, 255, 0.1)' }
                            }
                        },
                        plugins: {
                            legend: { labels: { color: '#FFFFFF' } }
                        }
                    }
                });
            }
            // Biểu đồ nhỏ - Doanh thu thức uống theo tháng
            if (revenueByMonthData.length > 0) {
                var ctxDrink = document.getElementById('monthlyDrinkChart').getContext('2d');
                new Chart(ctxDrink, {
                    type: 'bar',
                    data: {
                        labels: revenueByMonthData.map(item => item.Month),
                        datasets: [{
                            label: '@Localizer["DrinkRevenue"] (VND)',
                            data: revenueByMonthData.map(item => item.DrinkRevenue),
                            backgroundColor: 'rgba(40, 167, 69, 0.6)',
                            borderColor: 'rgba(40, 167, 69, 1)',
                            borderWidth: 1
                        }]
                    },
                    options: {
                        responsive: true,
                        scales: {
                            y: {
                                beginAtZero: true,
                                ticks: {
                                    callback: function(value) {
                                        return new Intl.NumberFormat('vi-VN', { style: 'currency', currency: 'VND' }).format(value);
                                    },
                                    color: '#FFFFFF'
                                },
                                grid: { color: 'rgba(255, 255, 255, 0.1)' }
                            },
                            x: {
                                ticks: { color: '#FFFFFF' },
                                grid: { color: 'rgba(255, 255, 255, 0.1)' }
                            }
                        },
                        plugins: {
                            legend: { labels: { color: '#FFFFFF' } }
                        }
                    }
                });
            }
            // Biểu đồ nhỏ - Doanh thu combo theo tháng
            if (revenueByMonthData.length > 0) {
                var ctxCombo = document.getElementById('monthlyComboChart').getContext('2d');
                new Chart(ctxCombo, {
                    type: 'bar',
                    data: {
                        labels: revenueByMonthData.map(item => item.Month),
                        datasets: [{
                            label: '@Localizer["ComboRevenue"] (VND)',
                            data: revenueByMonthData.map(item => item.ComboRevenue),
                            backgroundColor: 'rgba(153, 102, 255, 0.6)',
                            borderColor: 'rgba(153, 102, 255, 1)',
                            borderWidth: 1
                        }]
                    },
                    options: {
                        responsive: true,
                        scales: {
                            y: {
                                beginAtZero: true,
                                ticks: {
                                    callback: function(value) {
                                        return new Intl.NumberFormat('vi-VN', { style: 'currency', currency: 'VND' }).format(value);
                                    },
                                    color: '#FFFFFF'
                                },
                                grid: { color: 'rgba(255, 255, 255, 0.1)' }
                            },
                            x: {
                                ticks: { color: '#FFFFFF' },
                                grid: { color: 'rgba(255, 255, 255, 0.1)' }
                            }
                        },
                        plugins: {
                            legend: { labels: { color: '#FFFFFF' } }
                        }
                    }
                });
            }
        });
    </script>
}