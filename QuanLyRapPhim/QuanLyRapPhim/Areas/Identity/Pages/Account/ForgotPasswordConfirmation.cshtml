@page
@model ForgotPasswordConfirmationModel
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@{
    ViewData["Title"] = "Đã gửi email";
}

<div class="container-fluid vh-100 d-flex align-items-center justify-content-center position-relative overflow-hidden">
    <div class="frame-wrapper">
        <div class="left-section">
            <div class="card shadow-lg border-0 confirmation-card p-5">
                <div class="text-center mb-4">
                    <div class="email-icon mb-3">
                        <i class="fas fa-envelope-open-text" style="font-size: 4rem; color: #e50914;"></i>
                    </div>
                    <h2 class="text-white mb-3">Đã gửi email</h2>
                    <p class="text-white-50 mb-4">
                        Chúng tôi đã gửi một email chứa link đặt lại mật khẩu đến địa chỉ email của bạn.
                    </p>
                    <p class="text-white-50 mb-4">
                        Vui lòng kiểm tra hộp thư đến và nhấp vào link để đặt lại mật khẩu.
                    </p>
                    <div class="alert alert-info" style="background-color: #2a2a2a; border-color: #e50914; color: #ffffff;">
                        <i class="fas fa-info-circle me-2"></i>
                        <strong>Lưu ý:</strong> Email có thể mất vài phút để đến. Nếu không thấy email, vui lòng kiểm tra thư mục spam. Link sẽ hết hạn sau 1 giờ.
                    </div>
                    <div class="mt-4">
                        <a asp-page="./Login" class="btn btn-create">Quay lại đăng nhập</a>
                    </div>
                </div>
            </div>
        </div>
        <div class="right-section">
            <div class="intro-text">
                <h2 class="text-white">CinemaX</h2>
                <h3 class="text-white">Kiểm tra email của bạn!</h3>
            </div>
        </div>
    </div>
</div>

<style>
    body {
        background-color: #0F0F0F;
        margin: 0;
        font-family: 'Montserrat', sans-serif;
        overflow: hidden;
    }

    .vh-100 {
        height: 100vh;
    }

    .container-fluid {
        display: flex;
        width: 100%;
        height: 100%;
        align-items: center;
        justify-content: center;
    }

    .frame-wrapper {
        display: flex;
        width: 90%;
        max-width: 1200px;
        height: 80%;
        background: #2e2e2e;
        border-radius: 15px;
        border: 4px solid #2e2e2e;
        box-shadow: 0 0 20px rgba(46, 46, 46, 0.4);
        overflow: hidden;
    }

    .left-section {
        flex: 1;
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: #2e2e2e;
    }

    .confirmation-card {
        max-width: 500px;
        width: 100%;
        background: #1c1c1c;
        border-radius: 10px;
        border: 1px solid transparent;
        background-image: linear-gradient(rgba(28, 28, 28, 0.9), rgba(28, 28, 28, 0.9)), linear-gradient(45deg, #e50914, #0F0F0F);
        background-origin: border-box;
        background-clip: padding-box, border-box;
        z-index: 2;
    }

    .email-icon {
        animation: pulse 2s infinite;
    }

    @@keyframes pulse {
        0%, 100% {
            transform: scale(1);
        }
        50% {
            transform: scale(1.1);
        }
    }

    .text-white-50 {
        color: rgba(255, 255, 255, 0.7);
    }

    .btn-create {
        background: linear-gradient(90deg, #ff0033 0%, #e50914 100%);
        border: none;
        border-radius: 5px;
        padding: 12px 30px;
        font-size: 16px;
        color: #ffffff;
        font-weight: 600;
        transition: background 0.3s ease;
        text-decoration: none;
        display: inline-block;
    }

    .btn-create:hover {
        background: linear-gradient(90deg, #e6002e 0%, #d30912 100%);
        color: #ffffff;
    }

    .right-section {
        flex: 1;
        background: url('https://i.pinimg.com/originals/40/21/94/402194c8db6bf41861c1aef74a457117.gif') no-repeat center center/cover;
        position: relative;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .right-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.2);
        z-index: 1;
    }

    .intro-text {
        position: relative;
        z-index: 2;
        color: #ffffff;
        text-align: center;
        padding: 20px;
    }

    .intro-text h3 {
        font-size: 1.5rem;
        font-weight: 600;
        margin-bottom: 10px;
    }

    .intro-text h2 {
        font-size: 2rem;
        font-weight: 700;
        margin-bottom: 20px;
    }
</style>

